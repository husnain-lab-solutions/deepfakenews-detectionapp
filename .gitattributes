# Use Git LFS for large model weights (adjust threshold as needed)
ml_service/models/text-fakenews/*.safetensors filter=lfs diff=lfs merge=lfs -text

# Treat binary assets appropriately
*.png binary
*.jpg binary
*.jpeg binary
